<%= form_with(model: wh_sto_hdr, local: true) do |form| %>
  <% if wh_sto_hdr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wh_sto_hdr.errors.count, "error") %> prohibited this wh_sto_hdr from being saved:</h2>

      <ul>
        <% wh_sto_hdr.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
