<%= form_with(model: trn_wh_request_dtl, local: true) do |form| %>
  <% if trn_wh_request_dtl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trn_wh_request_dtl.errors.count, "error") %> prohibited this trn_wh_request_dtl from being saved:</h2>

      <ul>
        <% trn_wh_request_dtl.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
