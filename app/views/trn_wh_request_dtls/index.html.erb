<p id="notice"><%= notice %></p>

<h1>Trn Wh Request Dtls</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trn_wh_request_dtls.each do |trn_wh_request_dtl| %>
      <tr>
        <td><%= link_to 'Show', trn_wh_request_dtl %></td>
        <td><%= link_to 'Edit', edit_trn_wh_request_dtl_path(trn_wh_request_dtl) %></td>
        <td><%= link_to 'Destroy', trn_wh_request_dtl, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Trn Wh Request Dtl', new_trn_wh_request_dtl_path %>
