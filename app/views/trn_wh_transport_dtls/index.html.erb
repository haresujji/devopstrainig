<p id="notice"><%= notice %></p>

<h1>Trn Wh Transport Dtls</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trn_wh_transport_dtls.each do |trn_wh_transport_dtl| %>
      <tr>
        <td><%= link_to 'Show', trn_wh_transport_dtl %></td>
        <td><%= link_to 'Edit', edit_trn_wh_transport_dtl_path(trn_wh_transport_dtl) %></td>
        <td><%= link_to 'Destroy', trn_wh_transport_dtl, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Trn Wh Transport Dtl', new_trn_wh_transport_dtl_path %>
