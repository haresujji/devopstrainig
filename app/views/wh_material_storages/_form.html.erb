<%= form_with(model: wh_material_storage, local: true) do |form| %>
  <% if wh_material_storage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wh_material_storage.errors.count, "error") %> prohibited this wh_material_storage from being saved:</h2>

      <ul>
        <% wh_material_storage.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
